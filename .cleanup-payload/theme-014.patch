brand-hero,
 .nvx-brand-page > .nvx-brand-hero {
@@ -92,85 +68,16 @@ body.page-template-templatespage-sede-php .nvx-page__shell {
   overflow: hidden;
 }
 
-/*
- * Post-hero clinical intro is emitted as a normal brand-section
- * (.nvx-hero-intro) so gutters match Chamberí / hub / treatment sections.
- * Legacy .nvx-hero-context (if any cached HTML) keeps the same shell.
- */
-.nvx-page__content .nvx-hero-context,
-.nvx-brand-page > .nvx-hero-context {
-  width: 100%;
-  margin: 0;
-  padding-block: var(--nvx-pad-section);
-  background: var(--nvx-color-paper, var(--nvx-light, #fff));
-  color: var(--nvx-text-body);
-  box-sizing: border-box;
-}
-
-.nvx-page__content .nvx-hero-context__inner,
-.nvx-brand-page > .nvx-hero-context > .nvx-hero-context__inner {
-  width: var(--nvx-shell);
-  max-width: 100%;
-  margin-inline: auto;
-  padding-inline: var(--nvx-gutter-inner);
-  box-sizing: border-box;
-}
-
 .nvx-hero-intro .nvx-brand-readable {
   max-width: min(100%, var(--nvx-measure-wide, 72ch));
 }
 
-.nvx-page__content .nvx-brand-hero__media,
-.nvx-brand-page > .nvx-brand-hero .nvx-brand-hero__media {
-  margin: 0;
-  border-radius: 0;
-  background: var(--nvx-ink);
-}
-
-.nvx-page__content .nvx-brand-hero__media img,
-.nvx-brand-page > .nvx-brand-hero .nvx-brand-hero__media img {
-  width: 100%;
-  height: 100%;
-  max-height: none;
-  object-fit: cover;
-  border-radius: 0;
-}
-
-/*
- * Duplicate façade under hero — CSS fallback. Prefer PHP strip
- * (nvx_content_strip_duplicate_fachada); avoid relational selectors.
- */
-.nvx-brand-page .nvx-brand-section--fachada {
-  display: none;
-}
-
-/* Clinic hub intro columns (live: nvx-brand-page--clinicas) */
-.nvx-brand-page--clinicas .nvx-brand-section__inner {
-  width: var(--nvx-shell);
-  max-width: 100%;
-  margin-inline: auto;
-  padding-inline: var(--nvx-gutter-inner);
-  box-sizing: border-box;
-}
-
-.nvx-brand-page--clinicas .nvx-brand-grid {
-  margin-block: var(--nvx-space-4) var(--nvx-space-6);
-  align-items: stretch;
-}
-
 .nvx-brand-section--soft {
-  background: var(--nvx-surface-base, #f6f7f8);
-}
-
-.nvx-brand-page .nvx-brand-section {
-  padding-block: var(--nvx-pad-section);
+  background: var(--nvx-surface-base);
 }
 
 /* Valoración form stage — inspired by header media (fondo-formulario):
    accent → accent gradient / photo, card floating in quiet air. */
-.nvx-valoracion-page > .nvx-hubspot-form-section,
-.nvx-valoracion-page > #nvx-hubspot-form,
-.nvx-med-page > .nvx-hubspot-form-section,
 .nvx-form-stage {
   position: relative;
   isolation: isolate;
@@ -179,7 +86,7 @@ body.page-template-templatespage-sede-php .nvx-page__shell {
   padding-block: clamp(56px, 8vw, 104px) clamp(64px, 9vw, 120px);
   padding-inline: 0;
   overflow: hidden;
-  scroll-margin-top: 96px;
+  scroll-margin-top: var(--nvx-header-height);
   /* Fallback palette sampled from fondo-formulario.webp */
   background:
     linear-gradient(
@@ -191,8 +98,6 @@ body.page-template-templatespage-sede-php .nvx-page__shell {
   background-color: var(--nvx-form-stage-fallback);
 }
 
-.nvx-valoracion-page > .nvx-hubspot-form-section::before,
-.nvx-valoracion-page > #nvx-hubspot-form::before,
 .nvx-form-stage::before {
   content: "";
   position: absolute;
@@ -206,26 +111,15 @@ body.page-template-templatespage-sede-php .nvx-page__shell {
   pointer-events: none;
 }
 
-.nvx-valoracion-page > .nvx-hubspot-form-section::after,
-.nvx-valoracion-page > #nvx-hubspot-form::after,
 .nvx-form-stage::after {
   content: "";
   position: absolute;
   inset: 0;
   z-index: 0;
-  background:
-    linear-gradient(
-      180deg,
-      rgba(246, 247, 248, 0.18) 0%,
-      rgba(246, 247, 248, 0.08) 40%,
-      rgba(20, 22, 26, 0.06) 100%
-    );
+  background: var(--nvx-form-stage-overlay);
   pointer-events: none;
 }
 
-.nvx-valoracion-page > .nvx-hubspot-form-section .nvx-page-body__inner,
-.nvx-valoracion-page > #nvx-hubspot-form .nvx-page-body__inner,
-.nvx-med-page > .nvx-hubspot-form-section .