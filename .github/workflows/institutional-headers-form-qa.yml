name: Institutional Headers and Form QA

on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
    paths:
      - '.github/workflows/institutional-headers-form-qa.yml'
      - 'scripts/staging2/audit-institutional-headers-form.mjs'
      - 'scripts/staging2/visual-qa-common.mjs'
      - 'scripts/staging2/visual-qa-edge-preload.mjs'
      - 'wp-content/themes/nuvanx-medical/assets/css/nvx-site-coherence.css'
      - 'wp-content/themes/nuvanx-medical/assets/css/nvx-canonical-page-hero.css'
      - 'wp-content/themes/nuvanx-medical/template-parts/content/nvx-clinics-hub-github.php'
      - 'wp-content/themes/nuvanx-medical/template-parts/content/nvx-valoracion-github.php'
      - 'wp-content/themes/nuvanx-medical/template-parts/content/nvx-blog-archive.php'
      - 'wp-content/themes/nuvanx-medical/templates/template-contact.php'
      - 'wp-content/themes/nuvanx-medical/inc/nvx-equipo-page.php'

permissions:
  contents: read

jobs:
  institutional-qa:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      contains(github.event.pull_request.labels.*.name, 'deploy-staging2') &&
      (github.event.action != 'labeled' || github.event.label.name == 'deploy-staging2')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      EXPECTED_SHA: ${{ github.event.pull_request.head.sha }}
      BASE_URL: https://staging2.nuvanx.com
      EVIDENCE_DIR: institutional-headers-form-evidence
    steps:
      - name: Check out immutable PR head
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ env.EXPECTED_SHA }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'

      - name: Validate QA source
        run: node --check scripts/staging2/audit-institutional-headers-form.mjs

      - name: Configure strict SiteGround SSH
        env:
          STAGING2_SSH_HOST: ${{ secrets.STAGING2_SSH_HOST }}
          STAGING2_SSH_PORT: ${{ secrets.STAGING2_SSH_PORT }}
          STAGING2_SSH_USER: ${{ secrets.STAGING2_SSH_USER }}
          STAGING2_SSH_PRIVATE_KEY: ${{ secrets.STAGING2_SSH_PRIVATE_KEY }}
          STAGING2_SSH_KNOWN_HOSTS: ${{ secrets.STAGING2_SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          : "${STAGING2_SSH_HOST:?Missing STAGING2_SSH_HOST secret}"
          : "${STAGING2_SSH_USER:?Missing STAGING2_SSH_USER secret}"
          : "${STAGING2_SSH_PRIVATE_KEY:?Missing STAGING2_SSH_PRIVATE_KEY secret}"
          : "${STAGING2_SSH_KNOWN_HOSTS:?Missing STAGING2_SSH_KNOWN_HOSTS secret}"
          SSH_PORT="${STAGING2_SSH_PORT:-18765}"
          mkdir -p "$HOME/.ssh"
          chmod 700 "$HOME/.ssh"
          printf '%s\n' "$STAGING2_SSH_PRIVATE_KEY" > "$HOME/.ssh/staging2_key"
          printf '%s\n' "$STAGING2_SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/staging2_key" "$HOME/.ssh/known_hosts"
          cat > "$HOME/.ssh/config" <<EOF
          Host nvx-staging2
            HostName $STAGING2_SSH_HOST
            User $STAGING2_SSH_USER
            Port $SSH_PORT
            IdentityFile $HOME/.ssh/staging2_key
            IdentitiesOnly yes
            BatchMode yes
            StrictHostKeyChecking yes
            UserKnownHostsFile $HOME/.ssh/known_hosts
            ConnectTimeout 15
            ServerAliveInterval 15
            ServerAliveCountMax 2
          EOF
          chmod 600 "$HOME/.ssh/config"

      - name: Wait for exact staging2 SHA
        run: |
          set -euo pipefail
          for attempt in $(seq 1 90); do
            deployed="$(ssh nvx-staging2 "test -f /home/customer/www/staging2.nuvanx.com/public_html/wp-content/themes/nuvanx-medical/.nvx-deploy-sha && tr -d '\\r\\n' < /home/customer/www/staging2.nuvanx.com/public_html/wp-content/themes/nuvanx-medical/.nvx-deploy-sha || true")"
            if [[ "$deployed" == "$EXPECTED_SHA" ]]; then
              echo "Exact staging2 SHA ready: $deployed"
              exit 0
            fi
            echo "Waiting for $EXPECTED_SHA; current=${deployed:-absent}; attempt=$attempt"
            sleep 5
          done
          echo "Timed out waiting for exact staging2 SHA $EXPECTED_SHA" >&2
          exit 1

      - name: Audit institutional headers and HubSpot form
        env:
          NODE_OPTIONS: --import=${{ github.workspace }}/scripts/staging2/visual-qa-edge-preload.mjs
        run: |
          set -euo pipefail
          mkdir -p "$EVIDENCE_DIR"
          node scripts/staging2/audit-institutional-headers-form.mjs \
            2>&1 | tee "$EVIDENCE_DIR/qa.log"
          grep -Fq 'INSTITUTIONAL_HEADERS_FORM_QA_OK' "$EVIDENCE_DIR/qa.log"

      - name: Upload QA evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4
        with:
          name: institutional-headers-form-evidence-${{ github.run_id }}-${{ github.run_attempt }}
          path: institutional-headers-form-evidence/
          retention-days: 30
          if-no-files-found: warn
