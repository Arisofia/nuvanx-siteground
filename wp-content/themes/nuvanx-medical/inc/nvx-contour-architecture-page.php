<?php
/**
 * Contour Architecture™ compatibility shim.
 *
 * The public `/remodelacion-corporal-laser-madrid/` route is a governed
 * Signature architecture hub and is rendered canonically by
 * `nvx-signature-phase-pages.php`. A second editorial renderer previously
 * claimed the same WordPress page at a later `the_content` priority, replacing
 * the Signature hub after it had already rendered and removing required
 * decision, zone and valuation sections.
 *
 * Keep this file loadable because it is part of the historical bootstrap, but
 * deliberately register no page-owner or content filters here. This preserves
 * one source of truth and prevents future double ownership of the route.
 *
 * @package nuvanx-medical
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
