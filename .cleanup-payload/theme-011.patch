.nvx-hero__copy h1 {
-    max-width: none;
-    font-size: clamp(2.25rem, 9.5vw, 3.25rem);
+  .nvx-brand-hero__copy :is(.nvx-brand-kicker, .nvx-eyebrow),
+  .nvx-page-hero__copy .nvx-eyebrow {
+    max-width: 34ch;
+    margin-bottom: var(--nvx-space-2);
+    font-size: var(--nvx-type-kicker);
+    line-height: var(--nvx-lh-kicker);
   }
 
-  .nvx-brand-hero__copy .nvx-brand-actions,
-  .nvx-brand-hero__copy .nvx-home-hero-ctas,
-  .nvx-hero__copy .nvx-page__cta {
-    flex-direction: column;
-    align-items: stretch;
-    width: 100%;
+  .nvx-brand-hero__copy :is(.nvx-brand-hero__title, h1),
+  .nvx-page-hero__copy :is(h1, .nvx-heading) {
+    max-width: 16ch;
+    margin-bottom: var(--nvx-space-3);
+    font-size: clamp(2rem, 9vw, 2.75rem);
+    line-height: 1.04;
   }
 
-  .nvx-brand-hero__copy .nvx-brand-btn,
-  .nvx-brand-hero__copy .nvx-button,
-  .nvx-brand-hero__copy .nvx-btn {
+  .nvx-brand-hero__copy :is(
+    .nvx-brand-actions,
+    .nvx-home-hero-ctas,
+    .nvx-page__cta,
+    .nvx-cta-pair
+  ),
+  .nvx-page-hero__copy :is(.nvx-brand-actions, .nvx-page__cta) {
+    display: grid;
+    grid-template-columns: 1fr;
+    gap: var(--nvx-space-1);
     width: 100%;
+    margin-top: 0;
   }
-}
 
-/* ── Cards (blog shortcode / blog index) ───────────── */
-.nvx-card-grid {
-  display: grid;
-  grid-template-columns: repeat(12, minmax(0, 1fr));
-  gap: var(--nvx-gap-base);
-}
-
-.nvx-card {
-  grid-column: span 4;
-  min-width: 0;
-}
-
-.nvx-card__media {
-  display: block;
-  margin-bottom: var(--nvx-gap-base);
-  overflow: hidden;
-  border-radius: var(--nvx-radius-image);
-}
+  .nvx-brand-hero__copy :is(.nvx-brand-btn, .nvx-button, .nvx-btn),
+  .nvx-page-hero__copy :is(.nvx-button, .nvx-btn) {
+    width: 100%;
+    padding-inline: var(--nvx-space-2);
+  }
 
-.nvx-card__media img {
-  width: 100%;
-  aspect-ratio: 4 / 5;
-  object-fit: cover;
+  .nvx-brand-hero__copy .nvx-brand-meta {
+    max-width: 42ch;
+    margin-top: var(--nvx-space-2);
+    font-size: var(--nvx-type-small);
+    line-height: 1.45;
+    letter-spacing: 0.03em;
+    text-transform: none;
+  }
 }
 
-.nvx-card__title {
-  margin: 0 0 12px;
-  font-family: var(--nvx-serif);
-  font-size: var(--nvx-type-h3);
-  font-weight: 400;
-}
+@media (max-width: 480px) {
+  .nvx-brand-hero__copy,
+  .nvx-page-hero__copy {
+    padding-inline: var(--nvx-space-2);
+    padding-bottom: var(--nvx-space-3);
+  }
 
-.nvx-card__title a {
-  color: inherit;
-  text-decoration: none;
+  .nvx-brand-hero__copy :is(.nvx-brand-hero__title, h1),
+  .nvx-page-hero__copy :is(h1, .nvx-heading) {
+    font-size: clamp(1.875rem, 9vw, 2.375rem);
+  }
 }
 
-.nvx-card__body {
+/* Post-hero clinical introduction */
+.nvx-hero-intro__lead,
+.nvx-hero-intro__description {
+  max-width: var(--nvx-measure);
+  margin: 0;
   font-family: var(--nvx-sans);
-  font-size: var(--nvx-type-body);
-  line-height: var(--nvx-lh-body);
   color: var(--nvx-text-body);
 }
 
-@media (max-width: 980px) {
-  .nvx-card {
-    grid-column: span 6;
-  }
+.nvx-hero-intro__lead {
+  font-size: var(--nvx-type-lead);
+  line-height: var(--nvx-lh-lead);
 }
 
-@media (max-width: 680px) {
-  .nvx-card {
-    grid-column: 1 / -1;
-  }
+.nvx-hero-intro__description {
+  margin-top: var(--nvx-space-3);
+  font-size: var(--nvx-type-body);
+  line-height: var(--nvx-lh-body);
+  color: var(--nvx-text-muted);
 }
 
 /* ═══════════════════════════════════════════════════════════
@@ -616,14 +533,6 @@ article.nvx-page > .nvx-page-hero {
   color: var(--nvx-ink);
 }
 
-.nvx-catalog__inner {
-  width: var(--nvx-shell);
-  max-width: 100%;
-  margin-inline: auto;
-  padding-inline: var(--nvx-gutter-inner);
-  box-sizing: border-box;
-}
-
 .nvx-catalog__intro {
   padding-bottom: var(--nvx-space-6, 48px);
   margin-bottom: var(--nvx-space-8, 64px);
@@ -949,20 +858,12 @@ article.nvx