edia,
-  .nvx-editorial-hero__media,
-  .nvx-page-hero__media,
-  .nvx-hero__media
+  .nvx-brand-hero .nvx-brand-hero__media,
+  .nvx-page-hero .nvx-page-hero__media
 ) {
   background-color: var(--nvx-ink);
   background-image: none;
   box-shadow: none;
 }
 
-/*
- * Hide still imagery inside opening heroes.
- * Specificity must beat nvx-patterns-editorial.css
- * (.nvx-page-hero .nvx-page-hero__media img, .nvx-*-hero__media .nvx-media).
- * Include .nvx-media / .wp-post-image classes used on hero thumbnails.
- */
+/* Beat the canonical hero-media rules while remaining limited to still media. */
 body.nvx-hero-blackout :is(
   .nvx-brand-hero .nvx-brand-hero__media,
-  .nvx-editorial-hero .nvx-editorial-hero__media,
   .nvx-page-hero .nvx-page-hero__media,
-  .nvx-hero .nvx-hero__media,
-  .nvx-hero .nvx-brand-hero__media,
+  .nvx-blog-hero .nvx-blog-hero__media,
   .nvx-home-hero-stage
-) :is(img, picture, .nvx-media, .nvx-media--body, .wp-post-image) {
-  display: none;
-}
-
-/*
- * Journal: hide the opening featured-media slot entirely (stills only in practice).
- * Intentionally does not keep a black empty column; copy span is adjusted below.
- * display:none on this ancestor also hides any nested video — that is accepted
- * for this temporary mode (journal heroes are image/text, not video stages).
- */
-body.nvx-hero-blackout .nvx-blog-hero__media {
+) :is(img, picture, .wp-post-image) {
   display: none;
 }
 
-/*
- * With media gone, reuse the text-only desktop span so copy is not left
- * in a half-width column. On small screens restore full width (posts.css
- * only resets text-only heroes; this selector would otherwise win).
- */
-body.nvx-hero-blackout .nvx-blog-hero:not(.nvx-blog-hero--text-only) .nvx-blog-hero__copy {
-  grid-column: 2 / span 9;
-}
-
-@media (max-width: 960px) {
-  body.nvx-hero-blackout .nvx-blog-hero:not(.nvx-blog-hero--text-only) .nvx-blog-hero__copy {
-    grid-column: span 12;
-  }
-}
-
-/*
- * Keep video visible without forcing display:block (flex/grid-safe).
- * display:revert only affects the video element itself — it cannot override
- * display:none on an ancestor (e.g. journal .nvx-blog-hero__media above).
- */
+/* Do not force a display mode; preserve each video component's native layout. */
 body.nvx-hero-blackout :is(
   .nvx-brand-hero,
-  .nvx-editorial-hero,
   .nvx-page-hero,
-  section.nvx-hero,
+  .nvx-blog-hero,
   .nvx-home-hero-stage
 ) video {
   display: revert;
diff --git a/wp-content/themes/nuvanx-medical/assets/css/nvx-medical-review.css b/wp-content/themes/nuvanx-medical/assets/css/nvx-medical-review.css
deleted file mode 100644
index 358e599a8f54e9ccea948f65a5c073cdbb6c9e07..0000000000000000000000000000000000000000
--- a/wp-content/themes/nuvanx-medical/assets/css/nvx-medical-review.css
+++ /dev/null
@@ -1,49 +0,0 @@
-.nvx-medical-review {
-  width: var(--nvx-shell);
-  max-width: 100%;
-  margin-inline: auto;
-  margin-block: var(--nvx-gap-wide);
-  padding: var(--nvx-pad-card);
-  border: 1px solid var(--nvx-border-soft);
-  border-radius: var(--nvx-radius-card);
-  background: var(--nvx-surface-soft);
-  color: var(--nvx-text-body);
-}
-
-.nvx-medical-review__kicker {
-  margin: 0 0 var(--nvx-space-2);
-  color: var(--nvx-text-muted);
-  font-family: var(--nvx-sans);
-  font-size: var(--nvx-type-kicker);
-  font-weight: 700;
-  letter-spacing: var(--nvx-track-kicker);
-  line-height: 1.4;
-  text-transform: uppercase;
-}
-
-.nvx-medical-review__identity,
-.nvx-medical-review__date {
-  max-width: var(--nvx-measure);
-  margin: 0;
-  font-family: var(--nvx-sans);
-  font-size: var(--nvx-type-body);
-  line-height: var(--nvx-lh-body);
-}
-
-.nvx-medical-review__date {
-  margin-top: var(--nvx-space-2);
-  color: var(--nvx-text-muted);
-}
-
-.nvx-medical-review__reviewer {
-  color: var(--nvx-ink);
-  font-weight: 700;
-  text-decoration-line: underline;
-  text-decoration-thickness: 1px;
-  text-underline-offset: 0.2em;
-}
-
-.nvx-medical-review__reviewer:focus-v