name: Obsolete One-Time Workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Obsolete workflow retained only for safe removal.'
