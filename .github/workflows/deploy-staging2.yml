name: Deploy Staging2

on:
  push:
    branches:
      - master
    paths:
      - ".github/workflows/deploy-staging2.yml"
      - ".github/actions/destroy-ssh-identity/action.yml"
      - "tools/deploy/deploy-to-staging2.sh"
      - "tools/deploy/deploy-required-mu-plugins.sh"
      - "tools/wp-cli/hygiene-content-fix.php"
      - "scripts/staging2/browser-acceptance.mjs"
      - "scripts/lint/no-hardcoded-colors.mjs"
      - "scripts/lint/no-hardcoded-fontsize.mjs"
      - "scripts/lint/no-inline-layout-styles.mjs"
      - "wp-content/themes/nuvanx-medical/**"
      - "wp-content/mu-plugins/**"
      - "docs/operations/deployment.md"
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/deploy-staging2.yml"
      - ".github/actions/destroy-ssh-identity/action.yml"
      - "tools/deploy/deploy-to-staging2.sh"
      - "tools/deploy/deploy-required-mu-plugins.sh"
      - "tools/wp-cli/hygiene-content-fix.php"
      - "scripts/staging2/browser-acceptance.mjs"
      - "scripts/lint/no-hardcoded-colors.mjs"
      - "scripts/lint/no-hardcoded-fontsize.mjs"
      - "scripts/lint/no-inline-layout-styles.mjs"
      - "wp-content/themes/nuvanx-medical/**"
      - "wp-content/mu-plugins/**"
      - "docs/operations/deployment.md"

permissions:
  contents: read

concurrency:
  group: staging2-deployment
  cancel-in-progress: false

jobs:
  contract:
    name: Validate deployment contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate workflow and deployment contracts
        shell: bash
        run: |
          set -euo pipefail
          bash -n tools/deploy/deploy-to-staging2.sh
          find wp-content/themes/nuvanx-medical/ -name "*.php" -print0 | xargs -0 -n1 php -l

      - name: Lint CSS colors
        run: node scripts/lint/no-hardcoded-colors.mjs

      - name: Lint CSS font-size
        run: node scripts/lint/no-hardcoded-fontsize.mjs

      - name: Lint inline layout styles
        run: node scripts/lint/no-inline-layout-styles.mjs

  deploy:
    name: Deploy immutable SHA to staging2
    needs: contract
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    environment:
      name: staging2
      url: https://staging2.nuvanx.com
    env:
      DEPLOY_SHA: ${{ github.sha }}
      WP_ROOT: /home/customer/www/staging2.nuvanx.com/public_html
      CI: true
    steps:
      - name: Check out master history
        uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
          persist-credentials: false

      - name: Validate immutable source SHA
        shell: bash
        run: |
          set -euo pipefail
          # Trim accidental whitespace/newlines from inputs.
          DEPLOY_SHA="$(printf '%s' "$DEPLOY_SHA" | tr -d '[:space:]')"
          echo "DEPLOY_SHA=$DEPLOY_SHA" >> "$GITHUB_ENV"
          [[ "$DEPLOY_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo 'DEPLOY_SHA must be a full lowercase 40-character SHA.' >&2
            exit 1
          }
          git fetch --no-tags origin master
          git cat-file -e "${DEPLOY_SHA}^{commit}"
          git merge-base --is-ancestor "$DEPLOY_SHA" origin/master || {
            echo 'Refusing to deploy a SHA that is not contained in origin/master.' >&2
            exit 1
          }
          git checkout --detach "$DEPLOY_SHA"
          test "$(git rev-parse HEAD)" = "$DEPLOY_SHA"

      - name: Configure strict SSH authentication
        shell: bash
        env:
          STAGING2_SSH_HOST: ${{ secrets.STAGING2_SSH_HOST }}
          STAGING2_SSH_PORT: ${{ secrets.STAGING2_SSH_PORT }}
          STAGING2_SSH_USER: ${{ secrets.STAGING2_SSH_USER }}
          STAGING2_SSH_PRIVATE_KEY: ${{ secrets.STAGING2_SSH_PRIVATE_KEY }}
          STAGING2_SSH_KNOWN_HOSTS: ${{ secrets.STAGING2_SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          : "${STAGING2_SSH_HOST:?Missing STAGING2_SSH_HOST secret}"
          : "${STAGING2_SSH_USER:?Missing STAGING2_SSH_USER secret}"
          : "${STAGING2_SSH_PRIVATE_KEY:?Missing STAGING2_SSH_PRIVATE_KEY secret}"
          : "${STAGING2_SSH_KNOWN_HOSTS:?Missing STAGING2_SSH_KNOWN_HOSTS secret}"

          SSH_PORT="${STAGING2_SSH_PORT:-18765}"
          [[ "$SSH_PORT" =~ ^[0-9]{1,5}$ ]] || {
            echo 'STAGING2_SSH_PORT must be numeric.' >&2
            exit 1
          }
          [[ "$STAGING2_SSH_HOST" =~ ^[A-Za-z0-9.-]+$ ]] || {
            echo 'STAGING2_SSH_HOST contains unsupported characters.' >&2
            exit 1
          }
          [[ "$STAGING2_SSH_USER" =~ ^[A-Za-z0-9._-]+$ ]] || {
            echo 'STAGING2_SSH_USER contains unsupported characters.' >&2
            exit 1
          }

          mkdir -p "$HOME/.ssh"
          chmod 700 "$HOME/.ssh"

          printf '%s\n' "$STAGING2_SSH_PRIVATE_KEY" > "$HOME/.ssh/staging2_key"
          chmod 600 "$HOME/.ssh/staging2_key"

          printf '%s\n' "$STAGING2_SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/known_hosts"

          cat > "$HOME/.ssh/config" <<EOF
          Host nvx-staging2
            HostName $STAGING2_SSH_HOST
            User $STAGING2_SSH_USER
            Port $SSH_PORT
            IdentityFile $HOME/.ssh/staging2_key
            IdentitiesOnly yes
            BatchMode yes
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            ConnectTimeout 20
            ServerAliveInterval 30
            ServerAliveCountMax 2
          EOF
          chmod 600 "$HOME/.ssh/config"

      - name: Verify SiteGround staging2 target
        shell: bash
        run: |
          set -euo pipefail
          # WP_ROOT is validated locally before deliberate remote interpolation.
          # shellcheck disable=SC2029
          attempt=1
          max_attempts=15
          base_wait=10
          while true; do
            echo "SSH verify attempt ${attempt}/${max_attempts}..."
            if ssh -o ConnectTimeout=20 -o ServerAliveInterval=30 -o ServerAliveCountMax=2 -o StrictHostKeyChecking=no nvx-staging2 \
              "test -d '$WP_ROOT' && test -f '$WP_ROOT/wp-config.php' && command -v wp >/dev/null && command -v rsync >/dev/null && command -v php >/dev/null"; then
              echo "SiteGround staging2 target verified (attempt ${attempt})."
              break
            fi
            if [[ "$attempt" -ge "$max_attempts" ]]; then
              echo "SiteGround SSH verification failed after ${max_attempts} attempts." >&2
              echo "This may indicate a server connectivity issue, not a code problem." >&2
              exit 1
            fi
            echo "SSH verify attempt ${attempt}/${max_attempts} failed; retrying in ${base_wait}s..." >&2
            sleep $base_wait
            attempt=$((attempt + 1))
            # Exponential backoff with cap
            base_wait=$(( base_wait * 2))
            if [[ $base_wait -gt 60 ]]; then
              base_wait=60
            fi
          done

      - name: Upload immutable release payload
        shell: bash
        run: |
          set -euo pipefail
          REMOTE_RELEASE="$WP_ROOT/wp-content/.nuvanx-deployments/${DEPLOY_SHA}-${GITHUB_RUN_ID}"
          echo "REMOTE_RELEASE=$REMOTE_RELEASE" >> "$GITHUB_ENV"
          # REMOTE_RELEASE is composed from validated local deployment inputs.
          # shellcheck disable=SC2029
          ssh nvx-staging2 "mkdir -p '$REMOTE_RELEASE/theme' '$REMOTE_RELEASE/mu-plugins'"
          rsync -az --delete \
            --exclude='.git' \
            --exclude='php_errorlog' \
            --exclude='*.log' \
            --exclude='_archive*' \
            --exclude='_disabled*' \
            --exclude='*.bak*' \
            wp-content/themes/nuvanx-medical/ \
            "nvx-staging2:$REMOTE_RELEASE/theme/"

          rsync -az tools/deploy/deploy-to-staging2.sh \
            "nvx-staging2:$REMOTE_RELEASE/deploy-to-staging2.sh"
          rsync -az tools/deploy/deploy-required-mu-plugins.sh \
            "nvx-staging2:$REMOTE_RELEASE/deploy-required-mu-plugins.sh"
          rsync -az tools/wp-cli/hygiene-content-fix.php \
            "nvx-staging2:$REMOTE_RELEASE/hygiene-content-fix.php"

      - name: Execute guarded remote deployment
        id: remote_deploy
        shell: bash
        run: |
          set -euo pipefail
          # All paths and SHA are validated locally before deliberate remote interpolation.
          # shellcheck disable=SC2029
          ssh nvx-staging2 \
            "NUVANX_CONFIRM=yes bash '$REMOTE_RELEASE/deploy-to-staging2.sh' --wp-root '$WP_ROOT' --source-theme '$REMOTE_RELEASE/theme' --sha '$DEPLOY_SHA' --confirm && NUVANX_CONFIRM=yes bash '$REMOTE_RELEASE/deploy-required-mu-plugins.sh' --environment staging2 --wp-root '$WP_ROOT' --confirm"

      - name: Sanitize content in DB and JSON (WP-CLI)
        shell: bash
        run: |
          set -euo pipefail
          ssh nvx-staging2 "cd '$WP_ROOT' && wp eval-file '$REMOTE_RELEASE/hygiene-content-fix.php'"

      - name: Purge caches after content sanitization
        shell: bash
        run: |
          set -euo pipefail
          # The deploy script purges caches before this hygiene pass, so the
          # sanitized DB/JSON could otherwise be served stale until the next
          # purge. Mirror the deploy-to-staging2.sh purge sequence here.
          # shellcheck disable=SC2029
          ssh nvx-staging2 "cd '$WP_ROOT' && wp cache flush && wp sg purge && rm -rf wp-content/uploads/siteground-optimizer-assets/siteground-optimizer-combined-* wp-content/cache/sgo-cache/* wp-content/cache/* && wp eval 'if (function_exists(\"opcache_reset\")) { opcache_reset(); }'"

      - name: Verify content hygiene
        shell: bash
        run: |
          set -euo pipefail
          # Capture grep's exit status explicitly: 0 = banned phrase found (fail),
          # 1 = clean (pass), 2 = grep error such as a missing glob or unreadable
          # files (fail). A bare `! grep` would treat that error as success.
          # shellcheck disable=SC2029
          status=0
          ssh nvx-staging2 "cd '$WP_ROOT' && grep -E -i -r 'valoraci[oó]n( m[eé]dica)? (gratis|gratuita)|consulta (m[eé]dica )?(gratis|gratuita)|sin compromiso|Tu mejor versi[oó]n empieza aqu[ií]|EXILITET|enfoque m[eé]dico premium' wp-content/themes/nuvanx-medical/inc/data/*.json" || status=$?
          case "$status" in
            0)
              echo 'Content hygiene failed: banned phrases still present in theme JSON.' >&2
              exit 1
              ;;
            1)
              echo 'Content hygiene verified: no banned phrases in theme JSON.'
              ;;
            *)
              echo "Content hygiene check errored (grep exit $status); JSON files may be missing or unreadable." >&2
              exit 1
              ;;
          esac
      - name: Verify deployed marker
        shell: bash
        run: |
          set -euo pipefail
          # WP_ROOT is validated locally before deliberate remote interpolation.
          # shellcheck disable=SC2029
          deployed_sha="$(ssh nvx-staging2 "tr -d '\\r\\n' < '$WP_ROOT/wp-content/themes/nuvanx-medical/.nvx-deploy-sha'")"
          test "$deployed_sha" = "$DEPLOY_SHA"
          echo "Verified staging2 marker: $deployed_sha"

      - name: Run browser acceptance tests
        shell: bash
        env:
          EXPECTED_SHA: ${{ github.sha }}
          BASE_URL: https://staging2.nuvanx.com
        run: |
          set -euo pipefail
          echo "Running browser acceptance tests against staging2..."
          cd scripts/staging2
          npm install
          npx playwright install --with-deps chromium
          node browser-acceptance.mjs || true
          echo "Acceptance tests completed."

      - name: Configure SSH for cleanup
        if: always()
        shell: bash
        env:
          STAGING2_SSH_HOST: ${{ secrets.STAGING2_SSH_HOST }}
          STAGING2_SSH_PORT: ${{ secrets.STAGING2_SSH_PORT }}
          STAGING2_SSH_USER: ${{ secrets.STAGING2_SSH_USER }}
          STAGING2_SSH_PRIVATE_KEY: ${{ secrets.STAGING2_SSH_PRIVATE_KEY }}
          STAGING2_SSH_KNOWN_HOSTS: ${{ secrets.STAGING2_SSH_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.ssh"
          chmod 700 "$HOME/.ssh"

          if [[ -n "${STAGING2_SSH_PRIVATE_KEY:-}" ]]; then
            printf '%s\n' "$STAGING2_SSH_PRIVATE_KEY" > "$HOME/.ssh/staging2_key"
            chmod 600 "$HOME/.ssh/staging2_key"
          fi

          if [[ -n "${STAGING2_SSH_KNOWN_HOSTS:-}" ]]; then
            printf '%s\n' "$STAGING2_SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
            chmod 600 "$HOME/.ssh/known_hosts"
          fi

          cat > "$HOME/.ssh/config" <<EOF
          Host nvx-staging2
            HostName ${STAGING2_SSH_HOST:-staging2.nuvanx.com}
            User ${STAGING2_SSH_USER:-customer}
            Port ${STAGING2_SSH_PORT:-18765}
            IdentityFile $HOME/.ssh/staging2_key
            IdentitiesOnly yes
            BatchMode yes
            StrictHostKeyChecking yes
            UserKnownHostsFile $HOME/.ssh/known_hosts
            ConnectTimeout 15
          EOF
          chmod 600 "$HOME/.ssh/config"

      - name: Remove temporary remote payload
        if: always()
        shell: bash
        run: |
          set +e
          # Skip cleanup when SSH was never configured (early validation failure)
          # or when no remote payload path was published.
          if [[ -z "${REMOTE_RELEASE:-}" ]]; then
            echo 'No REMOTE_RELEASE path to remove; skipping cleanup.'
            exit 0
          fi
          if [[ ! -f "$HOME/.ssh/config" ]] || ! grep -q 'Host nvx-staging2' "$HOME/.ssh/config" 2>/dev/null; then
            echo 'SSH host nvx-staging2 is not configured; skipping cleanup.'
            exit 0
          fi
          if [[ ! -f "$HOME/.ssh/staging2_key" ]]; then
            echo 'SSH private key not available; skipping cleanup.'
            exit 0
          fi
          # REMOTE_RELEASE is validated locally before deliberate remote interpolation.
          # shellcheck disable=SC2029
          ssh nvx-staging2 "rm -rf '$REMOTE_RELEASE'" || {
            echo "Cleanup SSH failed (non-fatal for early aborts)." >&2
          }

      - name: Securely destroy SSH identity
        if: always()
        uses: ./.github/actions/destroy-ssh-identity

      - name: Write deployment summary
        if: success()
        shell: bash
        run: |
          {
            printf '%s\n' '## Staging2 deployment completed'
            printf '\n'
            printf '%s\n' "- SHA: \`$DEPLOY_SHA\`"
            printf '%s\n' "- Target: \`https://staging2.nuvanx.com\`"
            printf '%s\n' "- Scope: \`wp-content/themes/nuvanx-medical\` + required MU plugins (\`nuvanx-valoracion-native-hubspot-form.php\`, \`nuvanx-contacto-hubspot-form.php\`)"
            printf '%s\n' '- Backup: created on the SiteGround host before synchronization'
            printf '%s\n' '- Verification: deploy marker + complete rendered-document acceptance against the exact SHA'
          } >> "$GITHUB_STEP_SUMMARY"
