name: Design System Exceptions

on:
  push:
    branches:
      - master
    paths:
      # Nested asset globs so future css/js subdirs stay in scope.
      - 'wp-content/themes/nuvanx-medical/assets/css/**'
      - 'wp-content/themes/nuvanx-medical/assets/js/**'
      # Full theme PHP: enqueue lives in functions.php + inc/*, but audit-css.mjs
      # also scans every template for inline style= / <style> / !important.
      - 'wp-content/themes/nuvanx-medical/**/*.php'
      - 'scripts/design-system/audit-css.mjs'
      - '.github/workflows/design-system-exceptions.yml'
  pull_request:
    branches:
      - master
    paths:
      - 'wp-content/themes/nuvanx-medical/assets/css/**'
      - 'wp-content/themes/nuvanx-medical/assets/js/**'
      - 'wp-content/themes/nuvanx-medical/**/*.php'
      - 'scripts/design-system/audit-css.mjs'
      - '.github/workflows/design-system-exceptions.yml'
  workflow_dispatch:

concurrency:
  group: design-system-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  active-system-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

      - name: Audit active stack and exception paths
        run: node scripts/design-system/audit-css.mjs

      - name: Publish exception inventory
        # Path-only audit JSON under qa/ — never upload theme PHP/JS/assets (size + secrets).
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: design-system-exceptions
          path: |
            qa/design-system/audit-summary.json
            qa/design-system/active-css-exceptions.json
          if-no-files-found: error
          retention-days: 14
