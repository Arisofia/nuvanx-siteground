name: Sonar Diagnostics

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to query'
        required: true
        default: 'master'
      project_key:
        description: 'Sonar project key'
        required: true
        default: 'Arisofia_nuvanx-siteground'

concurrency:
  group: sonar-diagnostic-${{ github.ref }}-${{ inputs.project_key }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  sonar:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'

      - name: Query quality gate status
        shell: bash
        env:
          TARGET_BRANCH: ${{ inputs.branch }}
          PROJECT_KEY: ${{ inputs.project_key }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          [ -n "${SONAR_TOKEN:-}" ] || { echo 'SONAR_TOKEN missing'; exit 1; }
          
          curl -fsS --retry 3 --max-time 30 --retry-max-time 120 \
            -H "Authorization: Bearer ${SONAR_TOKEN}" \
            --get --data-urlencode "projectKey=${PROJECT_KEY}" \
            --data-urlencode "branch=${TARGET_BRANCH}" \
            "https://sonarcloud.io/api/qualitygates/project_status" \
            | python -m json.tool

      - name: Query duplication contributors
        shell: bash
        env:
          TARGET_BRANCH: ${{ inputs.branch }}
          PROJECT_KEY: ${{ inputs.project_key }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          [ -n "${SONAR_TOKEN:-}" ] || { echo 'SONAR_TOKEN missing'; exit 1; }
          
          p=1
          while true; do
            curl -fsS --retry 3 --max-time 30 --retry-max-time 120 \
              -H "Authorization: Bearer ${SONAR_TOKEN}" \
              --get --data-urlencode "component=${PROJECT_KEY}" \
              --data-urlencode "branch=${TARGET_BRANCH}" \
              --data-urlencode "metricKeys=new_duplicated_lines_density,new_lines,new_duplicated_lines" \
              --data-urlencode "strategy=leaves" \
              --data-urlencode "ps=500" \
              --data-urlencode "p=$p" \
              "https://sonarcloud.io/api/measures/component_tree" -o "/tmp/tree_${p}.json"
            
            MORE=$(FILE="/tmp/tree_${p}.json" PAGE="$p" python - <<'PY'
          import json, os
          data = json.load(open(os.environ['FILE'], encoding='utf-8'))
          paging = data.get('paging', {})
          total = paging.get('total', 0)
          ps = paging.get('pageSize')
          if ps is None: ps = data.get('ps', 500)
          print('1' if int(total) > int(os.environ['PAGE']) * int(ps) else '0')
          PY
            )
            
            if [ "$MORE" = "0" ] || [ $p -ge 20 ]; then
              if [ $p -ge 20 ] && [ "$MORE" != "0" ]; then
                echo "WARNING: Reached 10,000 components limit. Results truncated." >&2
              fi
              break
            fi
            p=$((p+1))
          done
          
          python - <<'PY'
          import json, sys
          import glob
          rows=[]
          for f in glob.glob('/tmp/tree_*.json'):
              data=json.load(open(f,encoding='utf-8'))
              for c in data.get('components', []):
                  vals={}
                  for measure in c.get('measures',[]):
                      periods=measure.get('periods') or []
                      period=measure.get('period') or {}
                      if periods:
                          val = periods[0].get('value',0)
                      elif period:
                          val = period.get('value',0)
                      else:
                          val = measure.get('value',0)
                      vals[measure['metric']]=float(val or 0)
                  nl=vals.get('new_lines',0); nd=vals.get('new_duplicated_lines',0); dens=vals.get('new_duplicated_lines_density',0)
                  if nl or nd or dens:
                      rows.append((nd,dens,nl,c.get('path') or ''))
          print('DUPLICATION_CONTRIBUTORS')
          for nd,dens,nl,path in sorted(rows, reverse=True):
              print(json.dumps({'path':path,'new_lines':nl,'new_duplicated_lines':nd,'density':dens}, ensure_ascii=False))
          PY

      - name: Query security and reliability issues
        shell: bash
        env:
          TARGET_BRANCH: ${{ inputs.branch }}
          PROJECT_KEY: ${{ inputs.project_key }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          [ -n "${SONAR_TOKEN:-}" ] || { echo 'SONAR_TOKEN missing'; exit 1; }
          
          for quality in SECURITY RELIABILITY; do
            p=1
            while true; do
              curl -fsS --retry 3 --max-time 30 --retry-max-time 120 \
                -H "Authorization: Bearer ${SONAR_TOKEN}" \
                --get --data-urlencode "componentKeys=${PROJECT_KEY}" \
                --data-urlencode "branch=${TARGET_BRANCH}" \
                --data-urlencode "resolved=false" \
                --data-urlencode "softwareQualities=${quality}" \
                --data-urlencode "ps=500" \
                --data-urlencode "p=$p" \
                "https://sonarcloud.io/api/issues/search" -o "/tmp/${quality}_${p}.json"
              
              MORE=$(FILE="/tmp/${quality}_${p}.json" PAGE="$p" python - <<'PY'
          import json, os
          data = json.load(open(os.environ['FILE'], encoding='utf-8'))
          paging = data.get('paging', {})
          t = data.get('total')
          if t is None: t = paging.get('total', 0)
          ps = data.get('ps')
          if ps is None: ps = paging.get('pageSize', 500)
          print('1' if int(t) > int(os.environ['PAGE']) * int(ps) else '0')
          PY
              )
              
              if [ "$MORE" = "0" ] || [ $p -ge 20 ]; then
                if [ $p -ge 20 ] && [ "$MORE" != "0" ]; then
                  echo "WARNING: Reached 10,000 issues limit for ${quality}. Results truncated." >&2
                fi
                break
              fi
              p=$((p+1))
            done
            
            QUALITY="$quality" python - <<'PY'
          import json, os, sys
          import glob
          quality=os.environ['QUALITY']
          total_issues = 0
          all_issues = []
          for f in glob.glob(f'/tmp/{quality}_*.json'):
              data=json.load(open(f,encoding='utf-8'))
              if total_issues == 0:
                  total_issues = data.get('total', 0)
              all_issues.extend(data.get('issues', []))
          print(f"{quality} ISSUES RETRIEVED: {len(all_issues)} (Server side matches: {total_issues})")
          for issue in all_issues:
              impacts=issue.get('impacts') or []
              print(json.dumps({'rule':issue.get('rule'),'component':issue.get('component'),'line':issue.get('line'),'message':issue.get('message'),'impacts':impacts,'creationDate':issue.get('creationDate')},ensure_ascii=False))
          PY
          done

      - name: Query new-code quality metrics
        shell: bash
        env:
          TARGET_BRANCH: ${{ inputs.branch }}
          PROJECT_KEY: ${{ inputs.project_key }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          set -euo pipefail
          [ -n "${SONAR_TOKEN:-}" ] || { echo 'SONAR_TOKEN missing'; exit 1; }
          
          curl -fsS --retry 3 --max-time 30 --retry-max-time 120 \
            -H "Authorization: Bearer ${SONAR_TOKEN}" \
            --get --data-urlencode "component=${PROJECT_KEY}" \
            --data-urlencode "branch=${TARGET_BRANCH}" \
            --data-urlencode "metricKeys=new_duplicated_lines_density,new_security_rating,new_reliability_rating,new_maintainability_rating,new_lines,new_duplicated_lines" \
            "https://sonarcloud.io/api/measures/component" \
            | python -m json.tool
