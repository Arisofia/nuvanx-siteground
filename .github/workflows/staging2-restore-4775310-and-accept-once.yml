name: Staging2 Restore 4775310 and Accept Once

on:
  push:
    branches: [master]
    paths:
      - .github/workflows/staging2-restore-4775310-and-accept-once.yml

permissions:
  contents: read
  actions: read

# One controlled retry after a read-only diagnostic confirmed that the
# NUVANX-Staging-Boundary/1.0 agent can receive a real HTTP 200 from the edge.
jobs:
  restore-and-accept:
    uses: ./.github/workflows/deploy-staging2.yml
    with:
      sha: 4775310be2b7dcdda18f8fd6de8e40041c2eb555
      run_block_c_acceptance: true
    secrets: inherit
