name: Preview Audit

on:
  workflow_dispatch:

jobs:
  preview-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run audit script
        run: bash scripts/audit-theme.sh
